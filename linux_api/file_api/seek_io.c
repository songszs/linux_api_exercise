//
// Created by zs on 18-8-25.
//

#include <glob.h>
#include <zconf.h>

int main(int argc,char *argv[])
{
    size_t len;
    off_t offset;
    int fd,ap,j;
    char *buf;
    ssize_t numRead,numWritten;
//    if(argc<3 || set)
}